## hey programmer

Click on the <i className="fal fa-video text-lg"></i> walkthrough tab at the top of this
box to get started.

Write a function _greet_ that takes in a string argument, _s_, and returns the string: "hey s"

#### test_00:

```python
greet("alvin") # -> "hey alvin"
```

#### test_01:

```python
greet("jason") # -> "hey jason"
```

#### test_02:

```python
greet("how now brown cow") # -> 'hey how now brown cow'
```
